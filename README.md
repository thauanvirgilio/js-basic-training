# js-basic-training

My study notebook for JavaScript fundamentals.

Each file is a note, not a program: a short description of what I learned,
followed by the code I wrote while learning it. Snippets from different study
sessions sit side by side in the same file, so a note is meant to be read, not
executed.

I write the code by hand in a scratch file first, and move it here once I
understand it.

## Variables and conditionals

| Note | |
|---|---|
| [Checking if a number is even or odd](variables/even-or-odd.md) | `if` / `else`, modulo |
| [Checking if a number is a multiple of 3 or 5](variables/fizz-buzz.md) | chained `else if` |
| [Converting a numeric grade into a letter](variables/grade-converter.md) | chained `else if` |
| [Finding the largest of three values](variables/largest-of-three.md) | comparison operators |
| [Calculating BMI and classifying the result](variables/bmi.md) | arithmetic, ranges |

## Output

| Note | |
|---|---|
| [Four ways to show output in the browser](outputs/outputs.md) | `innerHTML`, `document.write`, `alert`, `console.log` |

## Functions

| Note | |
|---|---|
| [Summing the values of an array](functions/sum-array.md) | `for`, accumulator |
| [Reversing a string with a loop](functions/reverse-string.md) | reverse iteration |
| [Counting the vowels in a word](functions/count-vowels.md) | nested loops |
| [Filtering the even numbers from an array](functions/filter-even.md) | `push` |
| [Finding the largest value in an array](functions/find-largest-value.md) | function vs arrow function |
| [Building FizzBuzz from 1 to 100 with a function](functions/fizz-buzz.md) | `return` |

## Objects

| Note | |
|---|---|
| [Declaring an array of objects](objects/object-array.md) | the base data used by the notes below |

## Array methods

Each of these compares the manual `for` version with the method version.

| Note | |
|---|---|
| [Mapping an array of objects into a new array](array-methods/map.md) | `map` |
| [Filtering an array of objects by a condition](array-methods/filter.md) | `filter` |
| [Searching for a product by id](array-methods/find.md) | `find` |
| [Summing all prices with an accumulator](array-methods/reduce.md) | `reduce` |
| [Checking if one or all items match a condition](array-methods/some-and-every.md) | `some`, `every` |

## Destructuring

| Note | |
|---|---|
| [Destructuring arrays, objects and function parameters](destructuring/array-destructuring.md) | arrays, objects, rest, swapping |
